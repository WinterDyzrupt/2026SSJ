using Common.Data.Dialog;

namespace Common.Utilities
{
    public static class DialogHelper
    {
        public static void StartDialog(ScriptChunk dialog)
        {
            // show dialog prefab
            // send to dialog monobehaviour
                // foreach line in chunk:
                    // populate left and right participants
                    // populate dialog box
                    // wait for button press
        }
    }
}